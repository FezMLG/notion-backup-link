{
  "name": "notion-backup",
  "version": "2.0.1",
  "n8n-version": "0.179.0",
  "description": "",
  "main": "./src/notion-backup.ts",
  "engines": {
    "node": ">=16.15.0"
  },
  "scripts": {
    "start": "ts-node .",
    "compile": "tsc",
    "docker": "cd .docker && docker-compose build && docker-compose up -d"
  },
  "license": "MIT",
  "dependencies": {
    "axios": "^0.21.1",
    "commander": "^7.2.0",
    "dotenv-flow": "^3.2.0"
  },
  "devDependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^4.7.4"
  }
}
